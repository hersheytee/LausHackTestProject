haptics:
  buttonPress:
    DEFAULT: sharp_state_change
    MX Master 4: sharp_collision  # Optional: device-specific mappings
  dampHaptic:
    DEFAULT: damp_state_change
    MX Master 4: damp_collision  # Optional: device-specific mappings
  loadingHaptic:
    DEFAULT: ringing
    MX Master 4: ringing  # Optional: device-specific mappings